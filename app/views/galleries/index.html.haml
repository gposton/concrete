%table
  - @sets.each_slice(3) do |row|
    %tr
      - row.each do |set|
        %td
          %h3= link_to set.title, gallery_path(:id => set.id)
          %div= link_to (image_tag set.thumb), gallery_path(:id => set.id)

